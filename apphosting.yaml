# Settings to manage and configure a Firebase App Hosting backend.
# This file controls server-side infrastructure settings for deployment on Firebase.
# For more information, see: https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # The maximum number of server instances to automatically spin up
  # in response to increased traffic.
  maxInstances: 1
